# MyVoc 使用说明

## 简介

MyVoc 是一款命令行单词记忆训练工具，基于 **艾宾浩斯遗忘曲线** 智能调度复习，帮助你高效背单词。



**核心功能：**

- 批量录入英文单词，自动查询音标与中文释义
- 背诵模式（手动 / 自动两种节奏）
- 考核模式（看中文写英文，即时判对错）
- 自动错题重测
- 艾宾浩斯复习调度
- 单词删除

---

## 运行方式

所有命令均需通过 `python -m myvoc` 运行：

```bash
python -m myvoc <command> [options]
```

**命令一览：**

| 命令 | 说明 |
|------|------|
| `python -m myvoc add` | 录入单词 |
| `python -m myvoc learn` | 背诵模式 |
| `python -m myvoc test` | 考核模式 |
| `python -m myvoc del <word1> <word2> ...` | 删除单词 |

---

## 1. 录入单词 — `add`

### 用法

```bash
python -m myvoc add [--skip-unknown]
```

### 操作步骤

1. 运行 `python -m myvoc add` 进入录入模式
2. 逐行输入英文单词，每行可以输入多个（空格分隔）
3. 输入完成后：
   - **Windows**：按 `Ctrl+Z`，然后回车
   - **Unix/Linux/macOS**：按 `Ctrl+D`
4. 按 `q` 可随时退出

### 示例

```
[录入模式] 输入完成后按 Ctrl+D (Unix) / Ctrl+Z (Windows) 结束
--------------------------------------------------
> abandon ability
  [OK] abandon     /əˈbændən/  vt. 放弃，抛弃
  [OK] ability     /əˈbɪləti/  n. 能力，才能
> xyzunknown
  [xyzunknown] 未查询到释义，请手动输入中文释义：
  [OK] xyzunknown  (手动)  虚构词
--------------------------------------------------
录入完成：共录入 3 个单词
```

### 选项

- `--skip-unknown` — 遇到查不到释义的单词时直接跳过，不提示手动输入

### 说明

- 同一个单词重复录入会自动跳过（去重）
- 查不到的单词会提示手动输入中文释义
- 录入完成后自动关联到今天的"会话"，方便背诵和考核

---

## 2. 背诵模式 — `learn`

### 用法

```bash
python -m myvoc learn [--auto] [--count N]
```

### 操作步骤

1. 运行 `python -m myvoc learn`
2. 屏幕上依次显示单词的英文、音标和中文释义
3. 按 `Enter` 进入下一个单词，按 `q` 退出

### 示例

```
== 背诵模式 · 手动 · 第 3 / 50 个 ==
----------------------------------------

  abandon
  /əˈbændən/

  vt. 放弃，抛弃；遗弃
----------------------------------------
[回车] 下一个  [q] 退出
```

### 选项

| 选项 | 说明 |
|------|------|
| `--auto` | 自动模式：每个单词显示指定时长后自动切换（默认 5 秒，可在配置文件中修改） |
| `--count N` | 只背诵前 N 个单词 |

---

## 3. 考核模式 — `test`

### 用法

```bash
python -m myvoc test [--count N]
```

### 操作步骤

1. 运行 `python -m myvoc test`
2. 屏幕显示中文释义，输入对应的英文单词
3. 按 `Enter` 判对错，按 `q` 退出

### 示例 — 答对

```
== 考核模式 · 第 12 / 65 题 ==
--------------------------------------------------

  释义：放弃，抛弃

  请输入英文：abandon

  [正确] abandon
--------------------------------------------------
```

### 示例 — 答错

```
== 考核模式 · 第 12 / 65 题 ==
--------------------------------------------------

  释义：放弃，抛弃

  请输入英文：abandond

  [错误] 正确答案：abandon
  释义：放弃，抛弃
--------------------------------------------------
```

### 答题规则

| 结果 | 效果 |
|------|------|
| **答对** | 单词复习阶段 +1，下次复习间隔延长 |
| **答错** | 阶段回退 2 级，难度系数降低，该单词在本轮结束前**自动重测** |

考核队列优先级：**当天新学单词 > 到期复习单词**。

---

## 4. 删除单词 — `del`

### 用法

```bash
python -m myvoc del word1 word2 word3
```

### 示例

```bash
python -m myvoc del abandon xyzunknown
```

删除完成后会提示成功和未找到的单词数量。

> **注意**：删除会同时清除该单词的所有学习记录。

---

## 配置文件

配置文件位于项目根目录的 `config.json`，支持以下配置项：

### 背诵设置（learning）

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `auto_mode` | boolean | `false` | 背诵默认是否为自动模式 |
| `auto_interval_seconds` | number | `5` | 自动模式下每个单词显示秒数 |
| `show_phonetic` | boolean | `true` | 是否显示音标 |
| `show_meaning_first` | boolean | `false` | 背诵时先显示中文 |

### 考核设置（testing）

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `case_sensitive` | boolean | `false` | 大小写是否敏感 |
| `typo_tolerance` | number | `0` | 允许拼写容错字母数（0 = 精确匹配） |

### 艾宾浩斯参数（ebbinghaus）

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `base_intervals` | array | `[0, 1, 2, 4, 7, 15, 30]` | 各级阶段的基础间隔天数 |
| `max_stage` | number | `7` | 最高阶段，达到后不再主动推送 |
| `default_ease_factor` | number | `2.5` | 默认难度系数 |

### 词典设置（dictionary）

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `provider` | string | `"youdao"` | 词典源（youdao / jinshan / offline） |
| `timeout_seconds` | number | `5` | 查词超时秒数 |

### 数据库设置（database）

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `path` | string | `""` | 数据库路径，空字符串使用默认路径 `%APPDATA%\myvoc\myvoc.db` |

---

## 数据存储

- **数据库**：SQLite 单文件，默认位于 `注意数据库在C:\Users\fly2M\AppData\Local\myvoc\myvoc`
- **配置文件**：项目根目录 `config.json`

---

## 常见问题

### Q：查不到单词释义怎么办？

A：程序会提示你手动输入中文释义；如果想直接跳过，加 `--skip-unknown` 参数。

### Q：如何重新开始背诵？

A：运行 `python -m myvoc learn` 即可，它总是从今天新录入的单词开始。如果想背其他天录入的单词，后续版本会支持选择。

### Q：数据会丢失吗？

A：数据保存在 SQLite 数据库中，不会因为退出程序而丢失。手动删除数据库文件会清除所有数据。

---

## 计划中（TODO）

- [ ] 单词发音（TTS）
- [ ] 错词本专项复习
- [ ] 学习统计（`today` / `status` 命令）
- [ ] 导入 / 导出词表
- [ ] 模糊拼写容错
