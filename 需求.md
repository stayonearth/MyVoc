# MyVoc单词记忆软件基础需求与设计框架



## 介绍

本目录主要目的是开发一个单词记忆软件帮我背单词的CLI版本。因为我没有预先设置的词表，我想使用方法应该为：

1. 输入我今天要背的单词，只有英文单词。
2. 根据我的输入，软件在后台查询单词的音标、中文意义，构建词表。
3. 我全部输完（比如50个）后，点击结束按键（Ctrl+F）。
4. 然后我可以选择开始背单词，今天的50个词会依次显示（包含音标、读音（todo）、中文），我可以选择自动来，每个单词显示的时间由配置文件定义，也可以每次按enter，进入下一个。我可以随时结束记过程。
5. 考单词阶段，显示中文，等待我输入英文单词，回车后给出判定结果。在后数据库中标记背单词的结果。在考完当天输入的单词后，还需要根据艾宾浩斯曲线，选择之前出错的单词，或者巩固之前背诵的单词。



### 已覆盖的核心功能

1. ✅ 单词批量录入（纯英文输入）
2. ✅ 自动抓取音标与中文释义
3. ✅ 背诵模式（自动 / 手动切换）
4. ✅ 考核模式（中译英，正误判定）
5. ✅ 艾宾浩斯复习调度

### 建议补充的关键能力

|      补充点      | 必要性 |                 说明                 |
| :--------------: | :----: | :----------------------------------: |
|     单词去重     |   高   | 重复输入时复用已有数据，避免重复查询 |
|   错误单词标记   |   高   |     考核答错的单词进入高频复习池     |
|    进度持久化    |   高   |         中途退出后下次可继续         |
|   每日学习统计   |   中   |      新学数量、正确率、复习数量      |
| 单词查询失败兜底 |   高   |    查不到释义时允许手动补充或跳过    |
| 导出 / 导入词表  |   中   |            备份或迁移数据            |

### 关于 `Ctrl+F` 结束输入的问题

终端环境下 `Ctrl+F` 不是标准的 EOF 信号，**推荐使用 `Ctrl+D`（Unix）或 `Ctrl+Z`（Windows）**，这是终端标准的输入结束信号，Python 的 `sys.stdin` 原生支持。如果坚持用 `Ctrl+F`，需要监听键盘事件，会引入 `readline` 或 `pynput` 等额外依赖，跨平台兼容性也较差。

------

## 二、整体技术架构

### 技术选型



|       层级       |              选型               |                      理由                      |
| :--------------: | :-----------------------------: | :--------------------------------------------: |
|       语言       |          Python 3.10+           |          内置 `sqlite3`，类型提示完善          |
|     CLI 框架     |            **Click**            | 命令分组清晰，参数校验强，比 argparse 更易维护 |
|      数据库      |           **SQLite**            |   单文件、零部署、Python 内置，适合本地工具    |
|     配置文件     |            **YAML**             |      可读性好，支持注释，用 `PyYAML` 解析      |
|     单词查询     | 有道词典开放 API / 金山词霸 API |          免费、稳定、返回音标 + 释义           |
|     终端美化     |            **Rich**             |  彩色输出、进度条、表格、面板，体验远超 print  |
|   发音（TODO）   |     `pyttsx3` 或 `edge-tts`     |         离线 / 在线两种方案，后续接入          |
| 键盘监听（可选） |       `readline`（内置）        |             方向键历史、快捷键支持             |

### 模块划分（分层架构）

```
myvoc/
├── cli/            # 命令行入口与交互层
├── core/           # 业务逻辑（背诵、考核、艾宾浩斯调度）
├── dict/           # 词典查询适配器
├── db/             # 数据库访问层（DAO）
├── models/         # 数据模型（dataclass）
└── config.py       # 配置加载
```

------

## 三、数据库设计（SQLite）

### 表 1：`words` — 单词主表

所有单词只存一份，通过 `word` 字段唯一索引去重。

|     字段     |    类型     |             说明              |
| :----------: | :---------: | :---------------------------: |
|     `id`     | INTEGER PK  |           自增主键            |
|    `word`    | TEXT UNIQUE |       英文单词（唯一）        |
|  `phonetic`  |    TEXT     |     音标，如 `/əˈbændən/`     |
|  `meaning`   |    TEXT     | 中文释义，多个义项用 `;` 分隔 |
| `created_at` |  DATETIME   |         首次录入时间          |
|   `source`   |    TEXT     |    数据来源（api/manual）     |

### 表 2：`learning_records` — 学习记录表

**每条单词每天一条记录**，记录背诵与考核结果，驱动艾宾浩斯调度。

|        字段        |    类型    |                     说明                     |
| :----------------: | :--------: | :------------------------------------------: |
|        `id`        | INTEGER PK |                   自增主键                   |
|     `word_id`      | INTEGER FK |                关联 words.id                 |
|    `learn_date`    |    DATE    |                   学习日期                   |
|      `stage`       |  INTEGER   | 当前复习阶段（0 = 新学，1 = 第 1 次复习...） |
|   `ease_factor`    |    REAL    |              难度系数，默认 2.5              |
|     `interval`     |  INTEGER   |                 当前间隔天数                 |
| `next_review_date` |    DATE    |                 下次复习日期                 |
|  `correct_count`   |  INTEGER   |                 累计答对次数                 |
|   `wrong_count`    |  INTEGER   |                 累计答错次数                 |
|   `last_result`    |    TEXT    |        最近一次结果（correct/wrong）         |
|  `last_review_at`  |  DATETIME  |                 最近复习时间                 |

### 表 3：`daily_sessions` — 每日会话表

记录每天录入了哪些单词，用于 "今天的 50 个词" 的范围界定。

|      字段      |    类型     |               说明                |
| :------------: | :---------: | :-------------------------------: |
|      `id`      | INTEGER PK  |                                   |
| `session_date` | DATE UNIQUE |             会话日期              |
|   `word_ids`   |    TEXT     | JSON 数组，当日录入的单词 ID 列表 |
| `total_words`  |   INTEGER   |           当日单词总数            |

------

## 四、艾宾浩斯调度算法设计

### 基础间隔序列（标准艾宾浩斯）



```
阶段 0（新学） → 间隔 0 天（当天复习）
阶段 1 → 间隔 1 天
阶段 2 → 间隔 2 天
阶段 3 → 间隔 4 天
阶段 4 → 间隔 7 天
阶段 5 → 间隔 15 天
阶段 6 → 间隔 30 天
阶段 ≥7 → 标记为已掌握，不再主动推送
```

### 评分规则（简化版 SM-2）

考核时根据作答结果调整：

- **答对**：`stage += 1`，`interval = 基础间隔[stage] * ease_factor`
- **答错**：`stage = max(0, stage - 2)`，`ease_factor -= 0.2`，`interval = 1`
- `ease_factor` 下限 1.3，上限 3.0

### 每日复习池生成逻辑

1. 先取**今日新学单词**（当天录入的）
2. 再取 `next_review_date <= 今天` 的所有到期复习单词
3. 优先级：**错误单词 > 到期复习 > 新学单词**
4. 答错的单词当天会再次出现（强化纠正）

------

## 五、CLI 交互与 UI 设计

### 命令结构（Click 分组）

此处不明确是不是每次都要输入python myvoc add.

```
myvoc add        # 进入单词录入模式
myvoc learn      # 进入背诵模式
myvoc test       # 进入考核模式
myvoc today      # 查看今日学习情况
myvoc status     # 查看整体统计
myvoc config     # 查看/修改配置
```

### 1. 录入模式（myvoc add）

```
📝 单词录入模式（输入完成后按 Ctrl+D 结束）
--------------------------------------------------
> abandon
  ✓ 已添加：abandon /əˈbændən/ v. 放弃，抛弃
> ability
  ✓ 已添加：ability /əˈbɪləti/ n. 能力，才能
> xyzabc
  ✗ 未查询到释义，请手动输入中文：[用户输入]
--------------------------------------------------
📊 本次共录入 48 个单词，其中 2 个手动补充
```

### 2. 背诵模式（myvoc learn）

支持 `--auto` 参数开启自动模式，否则按回车下一个。



```
🧠 背诵模式 · 第 3 / 50 个
━━━━━━━━━━━━━━━━━━━━━━━━━━━

  abandon
  /əˈbændən/

  vt. 放弃，抛弃；遗弃

━━━━━━━━━━━━━━━━━━━━━━━━━━━
[回车] 下一个  [q] 退出
```

自动模式下显示倒计时进度条，时间到自动切换。

### 3. 考核模式（myvoc test）



```
📝 考核模式 · 第 12 / 65 个（今日新学 50 + 复习 15）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

中文：放弃，抛弃

请输入英文：abandon

  ✅ 正确！ /əˈbændən/
  当前阶段: 1 → 2  下次复习: 2天后

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[回车] 继续  [q] 保存进度并退出
```

答错时显示正确答案，并标记为重测：

```
  ❌ 错误
  正确答案: abandon /əˈbændən/
  释义: 放弃，抛弃；遗弃
  该单词将在本轮结束前再次出现
```

------

## 六、配置文件设计（config.yaml）

```
# 背诵设置
learning:
  auto_mode: false        # 默认手动模式
  auto_interval_seconds: 5 # 自动模式下每个单词显示时长
  show_phonetic: true     # 是否显示音标
  show_meaning_first: false # 背诵时先显示中文再出英文

# 考核设置
testing:
  case_sensitive: false   # 大小写不敏感
  typo_tolerance: 1       # 允许1个字母误差算对（可选）

# 艾宾浩斯参数
ebbinghaus:
  base_intervals: [0, 1, 2, 4, 7, 15, 30]
  max_stage: 7
  default_ease_factor: 2.5

# 词典源
dictionary:
  provider: youdao        # youdao / jinshan / offline
  timeout_seconds: 5

# 数据库路径
database:
  path: ~/.myvoc/myvoc.db
```

------

## 七、核心代码结构示例

### 项目目录

```
myvoc/
├── __init__.py
├── __main__.py          # python -m myvoc 入口
├── cli.py               # Click 命令定义
├── config.py            # 配置加载与单例
├── models.py            # Word, LearningRecord 数据类
├── database.py          # SQLite 连接与初始化
├── dao.py               # 数据访问对象
├── scheduler.py         # 艾宾浩斯调度器
├── dictionary/
│   ├── __init__.py
│   ├── base.py          # 抽象基类
│   └── youdao.py        # 有道 API 实现
└── ui.py                # Rich 渲染组件
```

### 关键类设计

**Scheduler（调度器核心）**：

- `get_today_queue()` → 生成今日学习队列（新学 + 复习）
- `update_record(word_id, is_correct)` → 更新学习记录
- `calculate_next_review(stage, ease_factor, is_correct)` → 计算下次复习时间

**DictionaryClient**：

- 策略模式，可切换不同词典源
- 失败降级：查不到时返回 None，由 CLI 层提示手动输入

------

## 八、原型开发优先级（MVP → 迭代）

### Phase 1（MVP，1-2 天）

1. SQLite 建表 + DAO 层
2. 有道词典 API 接入
3. `add` 命令：批量录入 + 自动查词
4. `learn` 命令：手动模式背诵
5. `test` 命令：基础考核 + 正误判定

### Phase 2（3-5 天）

1. 艾宾浩斯调度器
2. 复习池自动生成
3. 背诵自动模式 + 倒计时
4. `today` / `status` 统计命令
5. 配置文件生效

### Phase 3（完善）

1. 单词发音（TTS）
2. 错词本专项复习
3. 导入 / 导出词表
4. 学习数据可视化（终端图表）
5. 模糊拼写容错

------

## 九、词典 API 选型说明

**推荐优先用有道开放接口**：

- 接口：`https://dict.youdao.com/suggest?num=1&doctype=json&q=单词`
- 返回包含：音标、中文释义、词性
- 无需鉴权，请求频率限制宽松，个人使用足够

备选方案：

- 金山词霸开放 API（需要 appkey）
- 本地词库（如 stardict 格式，离线可用）
- 爬取网页（不推荐，稳定性差）

